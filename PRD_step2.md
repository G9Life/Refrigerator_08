# PRD Step 2: 재료 기반 레시피 생성

## 1. 목적
1단계에서 확정된 재료 목록을 기반으로 OpenRouter API의 `deepseek/deepseek-chat-v3.1:free` 모델을 사용해 사용자가 바로 조리할 수 있는 레시피를 추천한다.

## 2. 배경
사용자는 냉장고 속 재료를 확인한 뒤 "무엇을 만들 수 있는지" 알고 싶어 한다. 2단계는 재료 인식 결과를 실제 행동 가능한 레시피로 바꾸는 핵심 추천 단계다.

## 3. 목표
- 1단계에서 확정한 재료 목록을 입력으로 받는다.
- `deepseek/deepseek-chat-v3.1:free` 모델로 레시피 후보를 생성한다.
- 각 레시피에 필요한 재료, 부족한 재료, 조리 시간, 난이도, 조리 순서를 제공한다.
- 사용자가 원하는 레시피를 선택해 상세 내용을 볼 수 있다.
- 3단계에서 저장할 수 있도록 레시피 데이터를 구조화한다.

## 4. 비목표
- 개인 프로필 기반 추천 최적화는 3단계에서 다룬다.
- 장보기 결제, 배송, 외부 커머스 연동은 포함하지 않는다.
- 영양 성분의 의학적 정확성을 보장하지 않는다.
- 실시간 재고 관리 기능은 포함하지 않는다.

## 5. 주요 사용자 시나리오
1. 사용자가 1단계에서 인식된 재료 목록을 확인하고 확정한다.
2. 앱은 확정된 재료 목록을 레시피 생성 API로 보낸다.
3. 서버는 `deepseek/deepseek-chat-v3.1:free` 모델에 레시피 추천을 요청한다.
4. 모델은 3개 내외의 레시피 후보를 구조화된 JSON으로 반환한다.
5. 사용자는 추천 레시피 목록에서 하나를 선택한다.
6. 앱은 선택한 레시피의 조리 단계와 부족한 재료를 상세히 보여준다.

## 6. 기능 요구사항
- 재료 입력
  - 1단계에서 확정된 재료 목록을 입력으로 사용한다.
  - 사용자가 직접 재료를 추가하거나 삭제할 수 있어야 한다.
  - 재료별 수량 정보가 있으면 프롬프트에 포함한다.
- 레시피 생성 요청
  - API 키는 서버 환경 변수 `OPENROUTER_API_KEY`에서만 읽는다.
  - OpenRouter `/chat/completions` 엔드포인트를 사용한다.
  - 모델은 `deepseek/deepseek-chat-v3.1:free`를 사용한다.
  - 모델에는 한국어 응답을 요청한다.
- 추천 결과
  - 기본 추천 개수는 3개로 한다.
  - 각 레시피는 제목, 요약, 사용 재료, 부족한 재료, 조리 시간, 난이도, 조리 단계, 팁을 포함한다.
  - 냉장고에 있는 재료를 최대한 우선 사용한다.
  - 부족한 재료가 많은 레시피는 낮은 우선순위로 정렬한다.
- 상세 화면
  - 레시피 카드 목록을 보여준다.
  - 카드에는 제목, 예상 시간, 난이도, 부족한 재료 수를 표시한다.
  - 상세 화면에는 단계별 조리법을 표시한다.

## 7. API 설계 초안
### POST `/api/generate-recipes`
요청:
```json
{
  "ingredients": [
    {
      "name": "계란",
      "quantity_estimate": "6개"
    },
    {
      "name": "양파",
      "quantity_estimate": "1개"
    }
  ],
  "preferences": {
    "servings": 2,
    "max_cooking_time_minutes": 30,
    "cuisine": "any"
  }
}
```

응답:
```json
{
  "recipes": [
    {
      "title": "양파 계란 볶음밥",
      "summary": "간단한 재료로 빠르게 만들 수 있는 한 끼",
      "used_ingredients": ["계란", "양파"],
      "missing_ingredients": ["밥", "간장"],
      "cooking_time_minutes": 20,
      "difficulty": "easy",
      "steps": [
        "양파를 잘게 썬다.",
        "팬에 기름을 두르고 양파를 볶는다.",
        "계란과 밥을 넣고 함께 볶는다."
      ],
      "tips": ["간장은 마지막에 넣어 간을 맞춘다."]
    }
  ]
}
```

## 8. 모델 프롬프트 요구사항
- 냉장고에 있는 재료를 우선 사용하도록 지시한다.
- 부족한 재료는 명확히 분리해서 표시하도록 한다.
- 과도하게 많은 추가 재료가 필요한 레시피는 피하도록 한다.
- 응답은 JSON으로 반환하도록 요구한다.
- 한국어 사용자에게 자연스러운 레시피 이름과 조리법을 사용한다.
- 식품 안전상 위험한 조합이나 덜 익힌 재료 사용은 피하도록 한다.

## 9. 에러 처리
- 재료 목록이 비어 있으면 `400`을 반환하고 재료 입력을 요청한다.
- OpenRouter 호출 실패 시 재시도 가능한 오류를 보여준다.
- 무료 모델 rate limit 발생 시 잠시 후 다시 시도하도록 안내한다.
- 모델 응답이 JSON 파싱에 실패하면 레시피 생성 재시도 버튼을 제공한다.
- 추천 가능한 레시피가 부족하면 기본 재료 추가를 제안한다.

## 10. 데이터 구조
레시피 객체는 3단계 저장 기능을 고려해 다음 필드를 유지한다.
- `id`: 클라이언트 또는 서버에서 생성한 임시 ID
- `title`: 레시피 제목
- `summary`: 짧은 설명
- `used_ingredients`: 보유 재료 목록
- `missing_ingredients`: 부족한 재료 목록
- `cooking_time_minutes`: 예상 조리 시간
- `difficulty`: `easy`, `medium`, `hard`
- `steps`: 조리 단계 배열
- `tips`: 추가 팁 배열
- `created_at`: 생성 시각

## 11. 성공 지표
- 확정된 재료 목록으로 3개 내외의 레시피가 생성된다.
- 각 레시피에 부족한 재료가 명확히 표시된다.
- 사용자가 추천 결과에서 하나의 레시피를 선택해 상세 정보를 볼 수 있다.
- 생성된 레시피 데이터가 3단계 저장 기능에 넘길 수 있는 구조를 가진다.

## 12. 수용 기준
- Given 사용자가 재료 목록을 확정했을 때, When 레시피 생성을 실행하면, Then 최소 1개 이상의 레시피 후보가 표시된다.
- Given 레시피 후보가 표시될 때, Then 각 후보에는 조리 시간, 난이도, 부족한 재료가 포함된다.
- Given 사용자가 레시피를 선택했을 때, Then 상세 조리 단계가 표시된다.
- Given 모델 호출이 실패했을 때, Then 사용자는 실패 원인과 재시도 방법을 확인할 수 있다.

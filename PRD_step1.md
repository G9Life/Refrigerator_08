# PRD Step 1: 냉장고 이미지 재료 인식

## 1. 목적
사용자가 냉장고 사진을 업로드하면 OpenRouter API의 `google/gemma-3-27b-it:free` 모델을 사용해 사진 속 식재료를 인식하고, 이후 레시피 생성 단계에서 사용할 수 있는 구조화된 재료 목록을 만든다.

## 2. 배경
냉장고 기반 레시피 추천 서비스의 핵심 입력은 사용자가 보유한 재료다. 사용자가 직접 재료를 입력하는 부담을 줄이기 위해 이미지 인식으로 재료 후보를 자동 추출한다.

## 3. 목표
- 사용자가 웹에서 냉장고 이미지를 업로드할 수 있다.
- 업로드된 이미지를 서버에서 OpenRouter API로 전달한다.
- `google/gemma-3-27b-it:free` 모델을 통해 이미지 속 재료를 추론한다.
- 인식 결과를 재료명, 추정 수량, 신뢰도, 메모 형태로 정리한다.
- 사용자가 인식 결과를 확인하고 수정할 수 있다.

## 4. 비목표
- 식재료의 정확한 중량 측정은 포함하지 않는다.
- 유통기한 자동 판독은 1단계 범위에 포함하지 않는다.
- 여러 장의 이미지를 합쳐 재료를 중복 제거하는 기능은 이후 단계로 미룬다.
- 영수증, 바코드, 제품 라벨 OCR 최적화는 포함하지 않는다.

## 5. 주요 사용자 시나리오
1. 사용자가 웹 앱에서 냉장고 사진을 업로드한다.
2. 앱은 업로드된 이미지를 미리보기로 보여준다.
3. 사용자가 "재료 인식" 버튼을 누른다.
4. 서버는 이미지를 OpenRouter API로 전달한다.
5. 모델은 사진 속 재료 후보를 JSON 형식으로 반환한다.
6. 사용자는 인식된 재료 목록을 확인하고 잘못된 항목을 수정하거나 삭제한다.
7. 확정된 재료 목록은 2단계 레시피 생성 입력으로 전달된다.

## 6. 기능 요구사항
- 이미지 업로드
  - JPG, PNG, WebP 형식을 지원한다.
  - 최대 파일 크기는 초기 버전에서 10MB로 제한한다.
  - 업로드 전 클라이언트에서 이미지 미리보기를 제공한다.
- 이미지 인식 요청
  - API 키는 서버 환경 변수 `OPENROUTER_API_KEY`에서만 읽는다.
  - 브라우저 번들에 API 키가 포함되지 않아야 한다.
  - OpenRouter `/chat/completions` 엔드포인트를 사용한다.
  - 모델은 `google/gemma-3-27b-it:free`를 사용한다.
- 인식 결과 형식
  - 모델 응답은 가능한 한 JSON으로 요청한다.
  - 각 재료는 `name`, `quantity_estimate`, `confidence`, `notes` 필드를 가진다.
  - 인식이 불확실한 항목은 `confidence`를 낮게 표시한다.
- 결과 검토 UI
  - 사용자는 재료명을 수정할 수 있다.
  - 사용자는 재료를 추가하거나 삭제할 수 있다.
  - 수정된 목록을 다음 단계로 넘길 수 있다.

## 7. API 설계 초안
### POST `/api/recognize-ingredients`
요청:
```json
{
  "image": "data:image/jpeg;base64,..."
}
```

응답:
```json
{
  "ingredients": [
    {
      "name": "계란",
      "quantity_estimate": "6개",
      "confidence": 0.82,
      "notes": "계란판 일부가 보임"
    }
  ],
  "raw_model_response": "optional debug value in development only"
}
```

## 8. 모델 프롬프트 요구사항
- 모델에게 냉장고 사진에서 보이는 식재료만 추출하도록 요청한다.
- 보이지 않는 재료를 추측하지 않도록 명시한다.
- 결과는 JSON 배열로 반환하도록 요구한다.
- 한국어 재료명을 우선 사용하도록 요청한다.
- 확실하지 않은 항목은 notes에 이유를 남기도록 한다.

## 9. 에러 처리
- API 키가 없으면 서버는 `500`과 설정 오류 메시지를 반환한다.
- 이미지 형식이 잘못되면 `400`을 반환한다.
- OpenRouter 호출 실패 시 사용자에게 재시도 안내를 보여준다.
- 무료 모델 rate limit 발생 시 잠시 후 재시도 메시지를 표시한다.
- 모델 응답이 JSON 파싱에 실패하면 원문 응답을 기반으로 재시도하거나 사용자에게 수동 입력 화면을 제공한다.

## 10. 보안 및 개인정보
- `.env` 파일은 Git에 커밋하지 않는다.
- OpenRouter API 호출은 서버에서만 수행한다.
- 업로드 이미지는 초기 버전에서 영구 저장하지 않는다.
- 로그에는 API 키와 이미지 base64 원문을 남기지 않는다.

## 11. 성공 지표
- 사용자가 이미지를 업로드하고 재료 목록을 받을 수 있다.
- 일반적인 냉장고 사진에서 주요 재료 3개 이상을 인식할 수 있다.
- 사용자가 인식 결과를 수정한 뒤 2단계로 이동할 수 있다.
- API 키가 클라이언트 코드에 노출되지 않는다.

## 12. 수용 기준
- Given 사용자가 유효한 이미지를 업로드했을 때, When 재료 인식을 실행하면, Then 재료 후보 목록이 화면에 표시된다.
- Given 모델이 불확실한 재료를 반환했을 때, Then 해당 항목은 낮은 신뢰도 또는 메모와 함께 표시된다.
- Given OpenRouter rate limit이 발생했을 때, Then 사용자에게 재시도 가능한 오류로 안내한다.
- Given 사용자가 재료 목록을 수정했을 때, Then 수정된 목록이 2단계 입력으로 전달된다.

# PRD Step 3: 사용자 프로필 및 레시피 저장

## 1. 목적
사용자 프로필을 만들고, 2단계에서 생성한 레시피를 개인 저장 목록에 보관할 수 있게 한다. 저장된 레시피는 다시 열람, 검색, 삭제할 수 있어야 하며 이후 개인화 추천의 기반 데이터가 된다.

## 2. 배경
레시피 추천이 한 번의 결과로 끝나면 사용자는 마음에 드는 레시피를 다시 찾기 어렵다. 프로필과 저장 기능을 추가하면 사용자의 취향, 식단 제약, 저장 이력을 기반으로 더 유용한 서비스로 확장할 수 있다.

## 3. 목표
- 사용자가 간단한 프로필을 생성할 수 있다.
- 사용자는 2단계에서 생성된 레시피를 저장할 수 있다.
- 저장된 레시피 목록을 확인하고 상세 내용을 다시 볼 수 있다.
- 저장된 레시피를 삭제할 수 있다.
- 프로필 정보는 향후 레시피 생성 선호도 입력으로 사용할 수 있는 구조를 가진다.

## 4. 비목표
- 소셜 로그인, OAuth 연동은 초기 3단계 범위에 포함하지 않는다.
- 레시피 공유, 댓글, 팔로우 같은 커뮤니티 기능은 포함하지 않는다.
- 결제, 프리미엄 구독, 광고 기능은 포함하지 않는다.
- 여러 기기 간 실시간 동기화는 초기 버전에서 필수로 보지 않는다.

## 5. 주요 사용자 시나리오
1. 사용자가 이름 또는 닉네임을 입력해 프로필을 만든다.
2. 사용자는 알레르기, 선호 식단, 싫어하는 재료 같은 선택 정보를 입력한다.
3. 사용자는 2단계 레시피 상세 화면에서 "저장" 버튼을 누른다.
4. 앱은 현재 사용자 프로필에 레시피를 저장한다.
5. 사용자는 저장된 레시피 목록에서 이전 레시피를 다시 확인한다.
6. 사용자는 더 이상 필요 없는 레시피를 삭제한다.

## 6. 기능 요구사항
- 프로필 생성
  - 필수 입력값은 닉네임으로 한다.
  - 선택 입력값은 알레르기, 선호 식단, 싫어하는 재료, 기본 인원 수로 한다.
  - 초기 버전은 로컬 저장 또는 단일 사용자 DB 구조로 시작할 수 있다.
- 프로필 수정
  - 사용자는 닉네임과 선호 정보를 수정할 수 있다.
  - 수정된 선호 정보는 이후 레시피 생성 요청의 `preferences`에 반영할 수 있어야 한다.
- 레시피 저장
  - 2단계에서 생성된 레시피 객체를 저장한다.
  - 저장 시 생성에 사용된 재료 목록도 함께 저장한다.
  - 같은 레시피를 중복 저장하지 않도록 제목과 생성 시각 또는 ID 기준으로 처리한다.
- 저장 목록
  - 저장된 레시피를 최신순으로 표시한다.
  - 목록에서는 제목, 요약, 조리 시간, 난이도, 저장 시각을 보여준다.
  - 상세 화면에서는 전체 조리 단계와 사용 재료를 보여준다.
- 삭제
  - 사용자는 저장된 레시피를 삭제할 수 있다.
  - 삭제 전 확인 메시지를 제공한다.

## 7. API 설계 초안
초기 버전에서 백엔드 저장소를 사용하는 경우 다음 API를 기준으로 한다.

### POST `/api/profile`
요청:
```json
{
  "nickname": "성준",
  "allergies": ["새우"],
  "diet_preferences": ["고단백"],
  "disliked_ingredients": ["고수"],
  "default_servings": 2
}
```

응답:
```json
{
  "profile": {
    "id": "profile_123",
    "nickname": "성준",
    "allergies": ["새우"],
    "diet_preferences": ["고단백"],
    "disliked_ingredients": ["고수"],
    "default_servings": 2,
    "created_at": "2026-05-18T00:00:00.000Z",
    "updated_at": "2026-05-18T00:00:00.000Z"
  }
}
```

### POST `/api/saved-recipes`
요청:
```json
{
  "profile_id": "profile_123",
  "recipe": {
    "title": "양파 계란 볶음밥",
    "summary": "간단한 재료로 빠르게 만들 수 있는 한 끼",
    "used_ingredients": ["계란", "양파"],
    "missing_ingredients": ["밥", "간장"],
    "cooking_time_minutes": 20,
    "difficulty": "easy",
    "steps": ["양파를 잘게 썬다.", "계란과 밥을 넣고 볶는다."],
    "tips": ["간장은 마지막에 넣는다."]
  },
  "source_ingredients": ["계란", "양파"]
}
```

응답:
```json
{
  "saved_recipe": {
    "id": "recipe_123",
    "profile_id": "profile_123",
    "title": "양파 계란 볶음밥",
    "saved_at": "2026-05-18T00:00:00.000Z"
  }
}
```

### GET `/api/saved-recipes?profile_id=profile_123`
응답:
```json
{
  "recipes": [
    {
      "id": "recipe_123",
      "title": "양파 계란 볶음밥",
      "summary": "간단한 재료로 빠르게 만들 수 있는 한 끼",
      "cooking_time_minutes": 20,
      "difficulty": "easy",
      "saved_at": "2026-05-18T00:00:00.000Z"
    }
  ]
}
```

### DELETE `/api/saved-recipes/{recipe_id}`
응답:
```json
{
  "ok": true
}
```

## 8. 데이터 모델 초안
### Profile
- `id`: 사용자 프로필 ID
- `nickname`: 사용자 표시 이름
- `allergies`: 알레르기 재료 배열
- `diet_preferences`: 선호 식단 배열
- `disliked_ingredients`: 싫어하는 재료 배열
- `default_servings`: 기본 인원 수
- `created_at`: 생성 시각
- `updated_at`: 수정 시각

### SavedRecipe
- `id`: 저장된 레시피 ID
- `profile_id`: 프로필 ID
- `title`: 레시피 제목
- `summary`: 짧은 설명
- `used_ingredients`: 사용 재료
- `missing_ingredients`: 부족한 재료
- `source_ingredients`: 레시피 생성 당시 입력 재료
- `cooking_time_minutes`: 예상 조리 시간
- `difficulty`: 난이도
- `steps`: 조리 단계
- `tips`: 추가 팁
- `saved_at`: 저장 시각

## 9. 개인정보 및 보안
- 알레르기와 식단 정보는 민감할 수 있으므로 로그에 남기지 않는다.
- 초기 로컬 저장 방식에서는 브라우저 저장소에 민감 정보가 남는다는 점을 사용자에게 안내한다.
- 서버 저장 방식을 사용할 경우 프로필 ID 기반 접근 제어를 고려한다.
- OpenRouter API 키는 이 단계에서도 서버 환경 변수로만 관리한다.

## 10. 향후 개인화 연계
- 프로필의 알레르기 재료는 2단계 레시피 생성 프롬프트에서 제외 재료로 사용한다.
- 싫어하는 재료는 추천 우선순위를 낮추는 조건으로 사용한다.
- 기본 인원 수는 레시피 생성 요청의 기본 `servings` 값으로 사용한다.
- 저장된 레시피 이력은 자주 선택하는 요리 스타일 분석에 활용할 수 있다.

## 11. 성공 지표
- 사용자가 프로필을 생성하고 수정할 수 있다.
- 사용자가 생성된 레시피를 저장할 수 있다.
- 저장된 레시피를 목록과 상세 화면에서 다시 확인할 수 있다.
- 저장된 레시피를 삭제할 수 있다.
- 프로필 정보가 2단계 레시피 생성 선호도와 연결될 수 있는 구조로 유지된다.

## 12. 수용 기준
- Given 사용자가 닉네임을 입력했을 때, When 프로필 생성을 실행하면, Then 프로필이 생성되고 이후 화면에서 유지된다.
- Given 사용자가 레시피 상세 화면을 보고 있을 때, When 저장 버튼을 누르면, Then 해당 레시피가 저장 목록에 추가된다.
- Given 저장된 레시피가 있을 때, When 저장 목록 화면을 열면, Then 최신순으로 레시피가 표시된다.
- Given 사용자가 저장된 레시피 삭제를 확인했을 때, Then 해당 레시피는 목록에서 제거된다.
- Given 사용자가 알레르기 정보를 입력했을 때, Then 이후 레시피 생성 요청에서 제외 조건으로 사용할 수 있다.
